# Regression Results Report Template

## Analysis Settings

The regression analysis was conducted in SmartPLS 4. The dependent variable was `[dependent variable]`. The independent variables were `[list predictors]`, and the control variables were `[list controls]`. The model was estimated with `[normal/HC3/HC4]` standard errors. Statistical significance was assessed using `[two-tailed/one-tailed]` tests at the `[0.05]` significance level.

## Model Fit

The model explained `[R-square]` of the variance in `[dependent variable]` (`R2 = ...`, adjusted `R2 = ...`).

## Regression Results Table

| Predictor | b | beta | SE | t | p | 95% CI | Decision |
|---|---:|---:|---:|---:|---:|---|---|
| Usefulness | | | | | | | |
| Ease of use | | | | | | | |
| Trust | | | | | | | |
| Service quality | | | | | | | |
| Age | | | | | | | |
| Experience | | | | | | | |

## Interpretation Paragraph

`[Predictor]` was positively/negatively associated with `[dependent variable]` (`b = ...`, `beta = ...`, `t = ...`, `p = ...`, 95% CI `[... , ...]`). Holding all other predictors constant, a one-unit increase in `[predictor]` was associated with a `[b]` unit increase/decrease in `[dependent variable]`.

## Diagnostics

The diagnostic output was inspected. The QQ plot `[did/did not]` indicate serious residual non-normality. Collinearity `[was/was not]` a concern. Robust standard errors `[were/were not]` used because `[reason]`.

## Conclusion

The results suggest that `[main conclusion]`. These findings should be interpreted as `[association/prediction]` because `[limitation]`.
